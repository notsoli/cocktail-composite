{
    "name": "distance",
    "description": "outputs the distance from each pixel to a specified point (defaults to 0,0)",
    "locals": [ "res" ],
    "inputs": [
        {
            "name": "position",
            "type": "vec2f",
            "default": "vec2(0., 0.)",
            "display": true
        },
        {
            "name": "point",
            "type": "vec2f",
            "default": "vec2(0., 0.)"
        }
    ],
    "outputs": [
        {
            "name": "distance",
            "type": "f32"
        }
    ],
    "function": "fn distance(pos : vec2f, point: vec2f) -> f32 { return sqrt((pos.x - point.x) * (pos.x - point.x) + (pos.y - point.y) * (pos.y - point.y)); }",
    "inline": "let $o{distance} = distance($i{position}, $i{point});"
}